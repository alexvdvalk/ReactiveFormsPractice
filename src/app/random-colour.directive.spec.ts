import { RandomColourDirective } from './random-colour.directive';

describe('RandomColourDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomColourDirective();
    expect(directive).toBeTruthy();
  });
});
